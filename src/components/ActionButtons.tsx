'use client';

import { motion } from 'framer-motion';

interface ActionButtonsProps {
  mediaId: string;
  isActive: boolean;
}

export function ActionButtons({ mediaId, isActive }: ActionButtonsProps) {
  // All action buttons removed as requested
  return null;
}
